# Redireccion canónica para joymarperu.com (sin www + HTTPS)
# Ajusta rutas de certificados y upstream segun tu servidor.

server {
    listen 80;
    listen [::]:80;
    server_name joymarperu.com www.joymarperu.com;

    return 301 https://joymarperu.com$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name www.joymarperu.com;

    ssl_certificate /etc/letsencrypt/live/joymarperu.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/joymarperu.com/privkey.pem;

    return 301 https://joymarperu.com$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name joymarperu.com;

    ssl_certificate /etc/letsencrypt/live/joymarperu.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/joymarperu.com/privkey.pem;

    add_header Strict-Transport-Security "max-age=31536000; includeSubDomains; preload" always;

    location / {
        proxy_pass http://127.0.0.1:3018;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Host $host;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
